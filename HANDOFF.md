# physiolog 인계 안내 (물리치료사용)

> 이 문서는 친구(현직 물리치료사)가 실사용을 시작할 때 필요한 모든 절차를 모은 가이드입니다.
> 평가자·개발자용 기술 문서는 [README.md](./README.md) 와 [PRD.md](./PRD.md) 참고.

---

## 0. 한 줄 요약

웹 브라우저에서 https://physiolog-collab.vercel.app 접속 → 가입 → 환자 등록 → 차팅 시작.

**별도 설치 없이 바로 쓸 수 있어요.** (zip 받아 로컬 실행은 인터넷 끊겨도 쓰고 싶을 때 옵션)

---

## 1. 첫 사용 (5분)

### 1-1. 가입
1. https://physiolog-collab.vercel.app 접속
2. 우상단 "가입" → 이메일·비밀번호 입력 (실제 메일 아니어도 OK, 본인 기억할 수 있는 거면 충분)
3. 로그인되면 빈 환자 리스트가 보임

### 1-2. 환자 등록
1. 우하단 ➕ 버튼
2. 13개 필드 중 **이름·생년월일·성별·진단명** 4개만 필수, 나머지는 비워도 저장됨
3. 저장 → 환자 정보 페이지로 자동 이동

### 1-3. 첫 치료 기록
1. 환자 정보에서 **[치료]** 탭
2. 우상단 ➕ "작성"
3. 부위 선택 → 방법 체크 → 코멘트 → 저장 (1~2분)

### 1-4. 같은 환자 두 번째 치료부터는 1클릭 복사
1. [치료] 탭 우상단 **"이전 기록 복사"** 클릭
2. 이전 회차 선택 → 모든 필드 자동 채워짐
3. 바뀐 부분(코멘트·운동 강도)만 수정 → 저장 (**30초 ~ 1분**)

---

## 2. 화면별 사용법

### 환자 리스트 `/`
- 상단 검색창: 이름으로 빠르게 찾기
- 상태 탭: 치료 중 / 종결 / 전체 — 종결된 환자를 숨겨서 깔끔하게
- 우상단 **선택 모드**: 여러 환자 한번에 삭제·상태 변경

### 환자 정보 `/patients/[id]`
- 우상단 ✏️ "환자정보수정": 13개 필드 편집
- 우상단 ⋮ 메뉴: 차트 출력(PDF) / 삭제
- 4탭 구조:
  - **기본정보** — 환자 기본 데이터
  - **치료** — 치료 기록 목록 + ➕ 작성 + 📋 이전 기록 복사
  - **검사** — (Phase 2)
  - **평가** — VAS·ROM·MMT·신체계측 + 추이 그래프 + ICF AI 분석

### 평가 입력
1. [평가] 탭 → ➕ "새 평가"
2. **그날 측정한 항목만 토글 ON**
   - VAS (통증 0~10)
   - ROM (관절 각도)
   - MMT (근력 0~5)
   - 신체계측 (둘레·길이)
   - Custom 평가 (FMS·Berg·보행거리·Modified Ashworth 등 자유 추가)
3. 저장 → 그래프 자동 갱신

### ICF AI 분석 (차별 기능)
1. [평가] 탭 → ➕ "ICF 분석"
2. 환자 자유 텍스트 입력 (또는 평가 히스토리 자동 활용)
3. AI가 5도메인(신체기능·활동·참여·환경·개인)으로 분석 + 임상 추론 요약 출력
4. 영어 의료 용어(kinesiophobia 등)에 **밑줄** 표시 → hover 또는 tap 하면 정의 팝업
5. **빨간 박스로 Red Flag** (Cauda Equina, 악성 종양 의심 등 응급 신호) 자동 강조

### PDF 출력 (환자 인계·의뢰서)
1. 환자 정보 우상단 ⋮ → "차트 출력"
2. 두 가지 선택:
   - **환자용 요약지** — 환자가 집에서 보는 친근한 톤 (홈프로그램 포함)
   - **물리치료 의뢰서** — 의사·타 의료진용 격식 양식 (VAS 추이 그래프 임베딩됨)
3. "PDF 저장" 버튼 → 브라우저 인쇄 다이얼로그 → "PDF로 저장"

---

## 3. 폰에서 보기 (PWA 설치)

1. 폰 Safari/Chrome에서 https://physiolog-collab.vercel.app 접속 후 로그인
2. **iPhone (Safari):** 하단 공유 버튼 → "홈 화면에 추가"
3. **Android (Chrome):** 우상단 ⋮ → "앱 설치" 또는 "홈 화면에 추가"
4. 홈에 추가된 아이콘 누르면 앱처럼 전체화면으로 열림
5. **같은 계정으로 로그인하면 PC·폰 데이터 자동 동기화** (Supabase 클라우드 사용)

오프라인에서도 한번 열린 화면은 볼 수 있어요 (PWA Service Worker).

---

## 4. 데이터는 어디에 저장되나

- **클라우드(Supabase)** — 인증 사용자별로 격리되어 안전. 본인 데이터만 본인에게 보임 (Row Level Security)
- PC가 망가지거나 폰을 바꿔도 같은 계정으로 로그인하면 데이터 그대로
- 비밀번호 잊으면 가입 메일로 재설정 가능 (실제 메일 가입 시)

---

## 5. 로컬 실행 (인터넷 없이도 쓰고 싶을 때, 선택사항)

대부분의 경우 웹 접속이면 충분하지만, 인터넷이 불안정한 환경(병원 와이파이 끊김 등) 대비:

### 사전 준비
1. Node.js 20 이상 설치 — https://nodejs.org/ → LTS 다운로드 → 다음 다음 다음
2. `physiolog.zip` 압축 풀기

### 실행
- **Mac**: `setup.command` 더블클릭
  - "확인되지 않은 개발자" 경고 시 → 시스템 설정 → 보안 → "그대로 열기"
- **Windows**: `setup.bat` 더블클릭

처음 한 번만 1~2분 걸리고, 그 다음부터는 30초 안에 켜져요. 자동으로 http://localhost:3000 이 브라우저에 열림.

폰에서 보고 싶으면: 터미널에 뜨는 `Network: http://192.168.xxx.xxx:3000` 주소를 같은 와이파이 폰에서 입력.

**종료:** 터미널 창에서 `Ctrl + C`

---

## 6. 자주 막히는 곳

| 증상 | 해결 |
|---|---|
| 로그인 후 데이터 안 보임 | 가입한 계정과 로그인 계정이 다른지 확인 |
| 환자 등록 후 리스트에 안 뜸 | 새로고침 또는 홈으로 갔다가 다시 오기 |
| ICF AI 분석 "credit exceeded" | 알려주세요 (Anthropic 키 잔액 충전) |
| PDF 글자가 깨짐 | 브라우저 인쇄 다이얼로그에서 "더 보기" → 배경 색상 체크 |
| 모바일에서 PDF가 너무 작아 보임 | 정상. PC에서 보거나 인쇄/저장하면 A4로 깨끗하게 나옴 |
| 폰에서 데이터 안 보임 | 같은 계정으로 로그인했는지 + 인터넷 연결 확인 |

---

## 7. 피드백 보내는 법

- 막히는 곳 → **스크린샷 + 어떤 동작하던 중이었는지** 친구(개발자)에게 메시지
- 새로운 기능 요청 → 환영. 특히 차팅 시간을 더 줄이는 아이디어
- 임상 부정확한 부분 (용어·해부학적 순서·평가 기준 등) → 가장 중요한 피드백

---

## 8. 다음 단계 (Phase 2 후보, 친구 의견 우선)

피드백·실사용 데이터 기반으로 우선순위 결정:

- **AI × 그래프 추이 cross-reference** — VAS·ROM 그래프를 AI가 자동 인식해 ICF 분석에 반영
- **음성 입력 (STT)** — 핸즈프리 차팅
- **팀 공유·권한 관리** — 같은 병원 다른 치료사와 환자 공유
- **카카오톡 공유** — 환자에게 요약지 메시지 전송
- **자동 진료 알림** — 환자 다음 예약 리마인더

---

> physiolog는 친구의 실제 임상에서 5분 안에 환자 차팅을 가능하게 만드는 것이 목표입니다.
> 실사용해보고 부족한 부분 알려주세요 — 그게 가장 큰 도움입니다.
